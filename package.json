{
  "name": "js-notes-app",
  "version": "0.1.0",
  "private": true,
  "description": "Vanilla JS SPA",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
